# Publish scope is controlled primarily by package.json#files.
# Keep this file so npm does not fall back to .gitignore.

# Runtime/local agent state must never ship in npm packages.
.omx/
.omc/
.triphos/
plugins/**/.omx/
plugins/**/.omc/
plugins/**/.triphos/
