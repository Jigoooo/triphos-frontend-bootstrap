# Visual Baseline

Tracked screenshots in this directory are the source of truth for `pnpm verify:visual`.

- `starter-desktop.png` uses `/starter` at 1440x1200.
- `starter-mobile.png` uses `/starter` at 430x932.
