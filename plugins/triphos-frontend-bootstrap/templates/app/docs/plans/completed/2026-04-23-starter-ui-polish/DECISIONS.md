# Decisions

## 2026-04-23

- Keep `html { font-size: 62.5% }` unchanged and scale affected surfaces locally.
- Replace the home stats with meaningful starter metadata while preserving the existing hero text used by the harness.
- Use a paused `00:30` countdown plus determinate progress as the single timer/loading demo.
- Implement multi-select chip removal inside the existing compound API instead of adding a new prop surface.
- Update verification scripts and visual baselines alongside the route refactor rather than weakening existing checks.
