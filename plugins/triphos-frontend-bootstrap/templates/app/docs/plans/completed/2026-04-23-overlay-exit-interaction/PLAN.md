# Overlay Exit Interaction Fix

## Goal

Prevent alert dialogs, modal dialogs, and bottom sheets from blocking clicks during their exit animation window.

## Scope

- `shared/ui/alert-dialog`
- `shared/ui/modal-dialog`
- `shared/ui/bottom-sheet`
- verification only; no public API changes
