# Decisions

## 2026-04-23

- Keep `html { font-size: 62.5% }` unchanged.
- Fix the trigger ring with focus-visible semantics instead of suppressing all focus feedback.
- Use a local date-picker bottom-sheet portal on small screens so the existing date-picker context remains reactive.
- Raise only the clearly undersized home/toggle text instead of applying a broad global font-size change.
